version: "3"
services:
  atm-sync:
    build:
      context: atm-sync
      dockerfile: Dockerfile
    volumes:
      - ./atm-sync:/app/
      - atm-sync_node_modules:/app/node_modules
volumes:
  atm-sync_node_modules:
